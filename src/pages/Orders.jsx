// Replace only the line below in your existing Orders.jsx if you do not want to replace the whole page:
// <ClientSearchSelect clients={clients} value={String(form.client_id ?? '')} onChange={(id) => setForm((f) => ({ ...f, client_id: String(id ?? '').trim() }))} />
// And make the create button disabled when no buyer is selected:
// <button className="btn btn-primary" onClick={handleCreate} disabled={saving || !String(form.client_id ?? '').trim()}>
